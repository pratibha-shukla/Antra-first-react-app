# React + Vite

npm create vite@latest project-name -- --template react

npm install 
npm run dev

using dummy json data for todo list 

using "map" method for render to display list and "key" prop for unique naming tag 

add a toodo method "input box" render to input using "setInput"

